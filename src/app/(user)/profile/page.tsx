import ProfileContainer from "@/feature/(user)/profile/container/ProfileContainer";
import React from "react";

const page = () => {
  return (
    <>
      <ProfileContainer />
    </>
  );
};

export default page;
